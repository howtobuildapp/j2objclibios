//
//  Generated by the J2ObjC translator.  DO NOT EDIT!
//  source: /Users/zhangwen/Documents/zwdir/sync/workspace/howtobuildapp/j2objclibios/javasrc/com/google/gson/annotations/Until.java
//

#include "J2ObjC_header.h"

#pragma push_macro("INCLUDE_ALL_Until")
#ifdef RESTRICT_Until
#define INCLUDE_ALL_Until 0
#else
#define INCLUDE_ALL_Until 1
#endif
#undef RESTRICT_Until

#if !defined (GSONUntil_) && (INCLUDE_ALL_Until || defined(INCLUDE_GSONUntil))
#define GSONUntil_

#define RESTRICT_JavaLangAnnotationAnnotation 1
#define INCLUDE_JavaLangAnnotationAnnotation 1
#include "java/lang/annotation/Annotation.h"

@class IOSClass;

/*!
 @brief An annotation that indicates the version number until a member or a type should be present.
 Basically, if Gson is created with a version number that exceeds the value stored in the 
 <code>Until</code> annotation then the field will be ignored from the JSON output. This annotation
  is useful to manage versioning of your JSON classes for a web-service. 
 <p>
  This annotation has no effect unless you build <code>com.google.gson.Gson</code> with a 
 <code>com.google.gson.GsonBuilder</code> and invoke 
 <code>com.google.gson.GsonBuilder</code> method. 
 <p>Here is an example of how this annotation is meant to be used:</p>
  @code

  public class User {
    private String firstName;
    private String lastName;   
   &#64Until(1.1) private String emailAddress;
      &#64Until(1.1) private String password;
  } 
  
@endcode
  
 <p>If you created Gson with <code>new Gson()</code>, the <code>toJson()</code> and <code>fromJson()</code>
  methods will use all the fields for serialization and deserialization. However, if you created
  Gson with <code>Gson gson = new GsonBuilder().setVersion(1.2).create()</code> then the 
 <code>toJson()</code> and <code>fromJson()</code> methods of Gson will exclude the <code>emailAddress</code>
  and <code>password</code> fields from the example above, because the version number passed to the 
  GsonBuilder, <code>1.2</code>, exceeds the version number set on the <code>Until</code> annotation, 
 <code>1.1</code>, for those fields.
 @author Inderjeet Singh
 @author Joel Leitch
 @since 1.3
 */
@protocol GSONUntil < JavaLangAnnotationAnnotation >

@property (readonly) jdouble value;

@end

@interface GSONUntil : NSObject < GSONUntil > {
 @public
  jdouble value_;
}

@end

J2OBJC_EMPTY_STATIC_INIT(GSONUntil)

FOUNDATION_EXPORT id<GSONUntil> create_GSONUntil(jdouble value);

J2OBJC_TYPE_LITERAL_HEADER(GSONUntil)

#define ComGoogleGsonAnnotationsUntil GSONUntil

#endif

#pragma pop_macro("INCLUDE_ALL_Until")
